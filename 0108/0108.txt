참조값에 의한 인수 전달
call by value

값이 복사되어 전달됨!!!
-원본이 훼손되지 않는다

지역변수 전역변수
\
무명함수
lambda funtion
몸체만 있는 짧은 함수


모듈 

객체지향언어의 함수의 소속을 알아야댐
객체지향언어란 각각의 함수 <- 주인공은 함수양
변수들을 제어하는 함수들을 변수에 종속 or 별개

변수와 별개면

비중이 늘어남 함수를 호출할때 파라미터 외우기힘듬

파라미터가 없는거는 외우기 좋아 but 비슷한 역할을 함수를 여러개를 정의해야댐


지향이란? 도달하지 못했다
객체지향은 100프로 객체로 만들어진게 아님

리스트라는 객체에서 값들도 관리, 이값들을 제어할수있는 함수를 가지고있다

그 객체에서만 사용할수있는거를 메소드

클래스는 객체에 대한 설계도
인스턴스 <-  함수들을 호출해야지 실행이 되는거니까 int a  int가 클래스 a가 인스턴스

struct my str{
		

	}

struct mystr ST st가 인스턴스 mystr 이 클래스  

메모리 먹는놈이 인스턴스 메모리 안먹는놈이 클래스

캡슐화(은닉화) - 장점이면서 단점
장점이 훨씬많음 
-지적 재산권 보호(가지고있는데 오픈소스하고 거기에 대해 공개시키고 특허형식으로 지적재산권보호함)

단점이자 장점
-미리 만들어진 클래스, 메소드를 수정하지 못함


class 클래스 이름 :
	def 메소드1 (self, xxx) : <- 메소드 정의할때 self라고 정의 대상을 인자값으로 넣어 줘야댐


객체 생성 클래스를 만든다고 바로 실행 x
따라서 생성 해야댐

생성자 객체가 생성될때 객체를 기본값으로 초기화하는 특수한 메소드이다.

attribute 변수 접근금지

중계자를 두고 변수를 건드려야댐
