정적변수 만들어두면 특정상황에서 핵유리


####overloading!!
 - 같은 이름의 함수를 이함수에 들어가는 인자값이 다를때 그 둘을 같은함수로 취급해서 이 함수를 실행 시킬때 파라미터에 따라 구분할 수 있다.

python은 function overloading이 지원 안된다.

빼게된 이유 : 인자값의 data타입을 x 

내장함수가 적으면 대부분 객체지향프로그램이당!

10장 3슬라이드 빨간색은 다외웡

eval 함수 - 문자열을 수식계산
exec 함수 - 수식뿐만 아니라 모든 파이썬 문장을 받아서 실행한다.
	  - 원래부터 이 코드가 돌아가는거처럼 실행???